output:

```bash
$ ./a.out --input test
hi man!
mini cat is so cool right? :)
better than the orginal :P

$ ./a.out --help
Minicat - a mini version of "cat".
USAGE:
  --help: print this message.
  -i, --input=<file_name>: Select the file for concatenate.
  -t, --top: print first 10 line.
  -l, --line: add number line.
```